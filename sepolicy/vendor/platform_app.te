get_prop(platform_app, vendor_asus_prop)

get_prop(platform_app, vendor_camera_prop)

allow platform_app ois_device:chr_file rw_file_perms;

get_prop(platform_app, vendor_set_camera_prop)
set_prop(platform_app, vendor_set_camera_prop)

get_prop(platform_app, vendor_asus_sp_system_prop)

get_prop(platform_app, vendor_tencent_prop)

allow platform_app system_lib_file:file { read getattr lock open };
